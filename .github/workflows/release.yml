name: Release
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@master
    - name: Archive Release
      uses: thedoctor0/zip-release@main
      with:
        type: 'zip'
        filename: 'NibTweaks.zip'
        exclusions: '*.git*'
    - name: Create Release
      uses: ncipollo/release-action@v1
      with:
        artifacts: "NibTweaks.zip"
        tag: ${{ github.ref_name }}
        name: "New Release"
